spring:
  data:
    rest:
      basePath: /api/chat
  jpa:
    show-sql: true
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        jdbc:
          time_zone: UTC
      dialect: org.hibernate.dialect.PostgreSQLDialect
  liquibase:
    enabled: true
